= Aurt Macro define

== Define C ABI

=== Basic Type

Basic type is a type in C. I will list all types.

- type: u8 u16 u32 u64 i8 i16 i32 i64 usize isize
- string: u8 pointer with end of `\0`
- array of type: pointer and length(if type is u8, array is bytes).

=== Function signature convert

==== Convert 

=== Async function and callback

